Some code for working with gettext format. 
Gettext format commonly used for localization software/web interface.

Contains helpers that can do:
- convert po formatted-text to json;
- transform string literals back to po format.